# Browser Automation

Browser automation is optional.

Use it when:

- pages require interaction
- content is rendered client-side
- login/session context is required
- visual inspection matters

Avoid it when:

- RSS or static HTML is enough
- a search/read API already extracts content
- the workflow would become brittle

Do not commit browser profiles, cookies, sessions, screenshots with private data, or downloaded personal content.
