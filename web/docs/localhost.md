# Localhost

One simple path:

1. Create a frontend app in `web/`.
2. Read JSON from `web/src/data`.
3. Run the dev server.

For a static prototype, any framework works. Next.js is a good default but not required.
