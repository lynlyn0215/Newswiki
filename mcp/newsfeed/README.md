# Newsfeed MCP

Read-only MCP for recent information.

Tools:

- `latest_articles`
- `search_articles`
- `source_health`
- `trend_summary`
- `wiki_candidates`

The template reads fake JSON fixtures. A private instance can point it at SQLite, exported JSON, or a local API.
